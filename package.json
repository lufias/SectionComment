{
  "name": "sectioncomment",
  "displayName": "SectionComment",
  "description": "Generate clear ascii text comment to highlight section of code",
  "version": "0.0.1",
  "publisher": "LufiasDev",
  "engines": {
    "vscode": "^1.91.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:sectioncomment.createSectionComment"
  ],
  "main": "./extension.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/lufias/SectionComment.git",
    "directory": "master"
  },
  "contributes": {
    "commands": [{
      "command": "sectioncomment.createSectionComment",
      "title": "Create Section Comment"
    }]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.91.0",
    "@types/mocha": "^10.0.7",
    "@types/node": "20.x",
    "eslint": "^8.57.0",
    "typescript": "^5.4.5",
    "@vscode/test-cli": "^0.0.9",
    "@vscode/test-electron": "^2.4.0"
  }
}
